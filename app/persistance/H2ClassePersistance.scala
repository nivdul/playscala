package persistance

class H2ClassePersistance {
  // écrire les commande dont j'aurai besoin
  val addImage = SQL("insert ")
  val removeImage = SQL(update )
  val addAlbum = SQL(insert )
  val searchImage = SQL(select  )
  

}